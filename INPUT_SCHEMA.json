{
  "title": "Registryoffice - urls .csv file",
  "description": "URLs to start with",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrls": {
      "title": "Start URLs",
      "type": "array",
      "description": "URLs to start with",
      "editor": "requestListSources"
    }
  },
  "required": ["startUrls"]
}
